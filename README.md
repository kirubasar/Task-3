# Day 3 Task

1. Comparing Two JSON Without Order
    1. Comparing of two JSON without order is completed in the file [camparingJSON.js](./comparingJSON.js)
2. Display Flags For all The Countries
    1. Created html file and js folder then create script.js file in js folder and added script.js to html 
    2. In script.js added all the code to get the data from [restcountries API](https://restcountries.com/v3.1/all) and display all the Flags
    3. The code is in the [displayflags](./displayflags/) folder
3. Display Name, Region, Subregion, Population
    1. Created html file and js folder then create script.js file in js folder and added script.js to html 
    2. In script.js added all the code to get the data from [restcountries API](https://restcountries.com/v3.1/all) and display all the countries Name, Region, Subregion, Population
    3. The code is in the [displaydetails](./displaydetails/) folder 


# Day 6 Task
1. Write a Movie Class
   1. Created classmovie folder then create movie.js file in this folder. In movie.js inside write the code for the class Movie
   2. To get the data from [classmovie](./classmovie) folder and it can be excuted in vs code
2. Write a Circle Class
   1. Created classcircle folder than create three files in this folder. 
       1.concolor.js file within classcircle folder and the file inside write the Circle class with properties for radius and color and toString method 
       2.conradius.js file within same folder and the file inside write the Circle class with properties for radius and  getter, setter methods
       3.constructor.js file also within this folder and the file inside the Circle class with empty constructor and static getter, setter methods
    2.To get the data from [classcircle](./classcircle) folder and it can be excuted in vs code 
3. Write a Person Class
   1. Created classperson folder then create person.js file in this folder. In person.js inside write the code for the class Person
   2. To get the data from [classperson](./classperson) folder and it can be excuted in vs code 
4. Write a UberPrice Class
   1. Created classuber folder then create uber.js file in this folder. In uber.js inside write the code for the class uberPrice
   2. To get the data from [classuber](./classuber) folder and it can be excuted in vs code








# Day 7 Task
1. Display Asia continents From all the Countries
   1. 1. Created html file and js folder then create script.js file in js folder and added script.js to html 
    2. In script.js added all the code to get the data from [restcountries API](https://restcountries.com/v3.1/all) and display all the countries from Asia continents using Filter method 
    3. The code is in the [filtercontinent](./filtercontinent/) folder
2. Display Population From all the Countries
    1. Created html file and js folder then create script.js file in js folder and added script.js to html 
    2. In script.js added all the code to get the data from [restcountries API](https://restcountries.com/v3.1/all) and display all the countries with a population of less than 2 lakhs using Filter method  
    3. The code is in the [filterpopulation](./filterpopulation/) folder
3. Display Name, Capital, Flag   
    1. Created html file and js folder then create script.js file in js folder and added script.js to html 
    2. In script.js added all the code to get the data from [restcountries API](https://restcountries.com/v3.1/all) and display all the countries name, capital, flag, using forEach method
    3. The code is in the [foreachdetails](./foreachdetails/) folder
4. Display Total Population and US Dollars countries
   1. Created html file and js folder then create script.js file in js folder and added script.js to html 
   2. In script.js added all the code to get the data from [restcountries API](https://restcountries.com/v3.1/all) and display all the countries total population, US dollars using reduce and forEach method  
   3. The code is in the [reducepopulation](./reducepopulation/) folder


